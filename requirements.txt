# RIS Multi-User Optimization Framework Requirements

# Core computation and scientific computing
numpy>=1.24.0
matplotlib>=3.6.0

# Progress bars and utilities  
tqdm>=4.64.0

# Cerebras Cloud SDK for LLM integration (WORKING - Required for API)
cerebras_cloud_sdk>=1.50.0

# Dependencies of cerebras_cloud_sdk
httpx>=0.23.0
anyio>=3.5.0
pydantic>=2.10.0
typing-extensions>=4.10.0
distro>=1.7.0
sniffio>=1.3.0
certifi>=2025.8.0
httpcore>=1.0.0
h11>=0.16.0
idna>=3.10
annotated-types>=0.6.0
pydantic-core>=2.33.0

# Note: LangChain dependencies removed - framework now uses direct Cerebras SDK
# Note: ML dependencies (scikit-learn, faiss-cpu, sentence-transformers) removed 
#       - using simplified RAG with numerical similarity instead